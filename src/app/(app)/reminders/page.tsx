export default function RemindersPage() {
  return (
    <section>
      <h2 className="text-2xl font-semibold">Reminders</h2>
      <p className="mt-2 text-muted-foreground">Start with one-time reminders. Add recurrence after the basic flow works.</p>
    </section>
  );
}
